<div class="cards-container">
    {{#each mision}}
        <div class="card">
            <h3>{{imgAlt}}</h3>
            <img src="{{imgUrl}}" alt="{{imgAlt}}">
            <p>{{{body}}}</p>
        </div>
    {{/each}}
</div>

